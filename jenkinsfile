pipeline {
    agent { docker { image 'python:3.5.1' } }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                sh 'sudo pip install --upgrade pip'
                sh 'sudo pip install mysql-connector-python'
            }
        }
        stage('connect to DB') {
            steps {
                echo 'test'
            }
        }
    }
}

